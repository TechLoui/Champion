{
  "name": "champion-backend",
  "version": "1.0.0",
  "description": "Champion Saúde Animal — API backend (Railway)",
  "main": "server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "dependencies": {
    "@googleapis/drive": "^22.0.0",
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.1",
    "firebase-admin": "^12.2.0",
    "google-auth-library": "^11.0.2",
    "resend": "^3.5.0"
  },
  "overrides": {
    "qs": "6.16.0"
  }
}
